rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }

    // User data - allow read/write for applet users
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}
